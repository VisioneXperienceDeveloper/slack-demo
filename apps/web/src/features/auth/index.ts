// Auth feature slice entry point
// Currently using Clerk built-in components directly in app/ pages,
// but this slice can house custom auth logic, hooks, or UI components in the future.

export const AUTH_FEATURE_NAME = 'auth';
